numpy
pandas
pyarrow
torch
torchvision
transformers>=4.37,<6  # >=4.37 mới có SigLIP; <6 để chặn breaking change kiểu get_image_features
                       # đổi return type (BaseModelOutputWithPooling) đã gặp ở 5.15.0, xem embed.py
onnxruntime-gpu
onnx          # torch.onnx.export(dynamo=False) cần package này,
              # KHÔNG phải onnxruntime. Thiếu -> OnnxExporterError lúc export.
faiss-cpu
scenedetect
faster-whisper
paddleocr
ultralytics
boto3
psycopg[binary]
# Stage shot->scene / keyframe
opencv-python-headless
pillow
ffmpeg-python
# ASR tiếng Việt: cài từ git (không có trên PyPI):
#   pip install git+https://github.com/khanld/chunkformer.git
# Shot detect: thêm thư mục inference/ của TransNetV2-master vào PYTHONPATH
#   (repo: https://github.com/soCzech/TransNetV2)
